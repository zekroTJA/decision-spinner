export interface AppTheme {
  colors: {
    background: string;
    surface: string;
    border: string;
    text: string;
    textMuted: string;
    accent: string;
    accentText: string;
  };
}

export const theme: AppTheme = {
  colors: {
    background: "#14090d",
    surface: "#1f1218",
    border: "#3a1c26",
    text: "#efefef",
    textMuted: "#b8a8ad",
    accent: "#d11f57",
    accentText: "#14090d",
  },
};
